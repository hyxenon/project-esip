/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "scontent.fmnl17-5.fna.fbcdn.net",
      "files.edgestore.dev",
      "img.freepik.com",
    ],
  },
};

export default nextConfig;
